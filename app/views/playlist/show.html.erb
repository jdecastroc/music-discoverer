<% #@playlist.resources_list.each { |web_resource| %>
    
    <h2><%= #web_resource.name %></h2>
    
    <% #web_resource.songs.each { |song| %>
        <p><%= #song.name %></p>
    <% #} %>
    
<% #} %>

<% #@playlist.songs_list.each { |song| %>
        <p><%# song.url %></p>
<% #} %>